<h1> Posts </h1>

<% @posts.published.each do |post| %>
  <h2><%= link_to post.menu_label, post_path(slug: post.slug) %></h2>
<% end %>

<% @posts.unpublished.each do |post| %>
  <h2><%= link_to post.menu_label, post_path(slug: post.slug) %></h2>
<% end %>

